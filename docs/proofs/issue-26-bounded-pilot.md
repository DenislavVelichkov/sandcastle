# Issue #26 bounded pilot: incomplete calibration

**Disposition: retain fixed Sol High.** The seven-configuration comparison was not run, so there is no model ranking, savings estimate, adaptive pair, or policy qualification. No policy was activated.

The host froze the `0.12.0-dv8.22.0` release from source `b56bd6c26039e99946f0bafaaee1b334b4c11f5d`, Docker image `sha256:6669402f260401868ab26a8a17c2d591747111f9ed582ca0326ad992a1b8835d`, Codex CLI `0.156.1`, the seven model and effort settings, four historical commit pairs, role policy, grader bytes, slot order, and account window. The actual worker catalog contained all seven settings. The frozen manifest SHA-256 is `87a92fc9d0ecfc68c905a225dd309ef4914b0e451e08c22f748da2b93ce7b7f3`. It binds the host entry at commit `dc56c03` and is retained outside candidate worktrees at `/home/dv8/Projects/context-engineering/mattpocock/sandcastle-issue26-run/manifest.json`.

The host clock started at 2026-09-26 09:20:17 UTC. Calibration preparation took 2,141 ms; the shared controller recorded 24,761 ms and one measurement call. A live checkpoint and provider session were captured. One raw counter recorded 14,620 input and 185 output tokens, but the interrupted invocation's attributable total remained unknown because coverage was incomplete. The controller then refused resume with `Project reservation recovery is required`: the frozen entry lacked its required `recoverReservation` callback. State is `recovery-required`, with a paused task, a retained reservation and worktree, and an unfinished shared-budget invocation. No additional model call was made.

Calibration therefore did not prove resumed settings, cancellation completion, or token reconciliation. All 64 scheduled evaluations remain unrun: 28 fixed development, 28 fixed held-out, and 8 adaptive. Historical preflight was not reached; no base-failure or known-correction result is claimed. The installed report renderer produced `report.html`, `report.json`, and `evaluations.csv`, plus a companion `findings.md`, under `/home/dv8/Projects/context-engineering/mattpocock/sandcastle-issue26-run/report/`. Their SHA-256 values are `64564b8992da36cb1eba8a7cb87065a512332285fd4bb81ca981b005a92df2ae`, `81c7023dbffa51658e0a56e685eacae5235f8558611df96af62c735510ff48bd`, and `9c781f65b6368d0b85ab078e88feeb0a6e2782b58f5f2d6e550f249a93f7510a`. The native report's manifest status is **unverified** because no scored evaluation froze a host-conditions hash. Its CSV has a header and no evaluation rows.

The normal report command could not reserve its five-minute budget while the calibration invocation retained ownership. The outputs above were rendered passively from the installed package, so that rendering time is not included in the shared active-time counter. Commit `45147aa` lets the host report entry take that same passive path while retaining the unfinished invocation. The account had one seven-day primary window with 38% used at the last recorded reading. This coarse unchanged percentage does not establish zero cost.

Commit `8ebe656` adds the missing reservation callback for future use. It changes the host-entry bytes and **does not** repair, resume, or authorize replacement of this frozen pilot. Any later pilot needs a new explicit authorization, fresh frozen conditions, and a distinct budget and policy identity. This issue remains open because its comparison and preflight gates did not pass.

## Authorized follow-up: second pilot stopped at historical preflight

The owner's follow-up request authorized a distinct attempt. Its frozen manifest is `/home/dv8/Projects/context-engineering/mattpocock/sandcastle-issue26-run-v2/manifest.json`, SHA-256 `12ddbc78355ac5fe556ab4ec65453162c9119d02b730bbe47cb5301ae302d5c3`, with policy `issue26-seven-arm-v2`. The first pilot and its byte-bound evidence were left unchanged. The second pilot used the same installed `0.12.0-dv8.22.0` release and verified all seven worker settings before inference.

The four-hour host clock started at 2026-09-26 10:06:42 UTC, before 2,253 ms of calibration preparation. Calibration recorded four model invocations and 286,563 ms of controller active time, below its six-call and 15-minute caps. The live checkpoint had verified source and session restoration; resume completed the implementation and both required reviews, and the task was accepted. The canceled first implementation invocation remains explicitly unknown. The three later invocations have complete counter coverage, but the calibration's total attributable token cost is null. An unchanged 39% reading in the coarse seven-day account window does not imply zero consumption.

Historical preflight stopped on the first case, `stream-log`, before any comparison evaluation. The installed fixture exporter omitted `.sandcastle/.env.example` when recreating an answer-free Git history: the historical base tracks that file, while its `.gitignore` pattern `.env.*` matches it. The exported tree therefore differed from the frozen base tree. The shared ledger records `historical-fixture-preflight` as failed after 148 ms. The source repair changes the exporter to stage every file from the trusted Git archive, with a regression case for an ignored but tracked file. That repair is **not** in the frozen installed release and cannot alter this attempt's outcome.

All 64 comparison slots remain unrun (28 fixed development, 28 fixed held-out, 8 adaptive). No case was replaced, no pair was frozen, and no model ranking or savings estimate exists. The shared controller reports 286,743 ms active after calibration, failed preflight, and metered report generation; the 2,253 ms preparation is recorded separately. The retained report is under `/home/dv8/Projects/context-engineering/mattpocock/sandcastle-issue26-run-v2/report/`: `report.html` SHA-256 `f790a9d6c35aa4538ec749a61b224f574234b561a54fbe751a89e44d02c16387`, `report.json` SHA-256 `132a90a9a9a714439bdb0af15e75b5d9d4e7bd3ebd4b7d57eb32bf93a9318ed1`, and `evaluations.csv` SHA-256 `9c781f65b6368d0b85ab078e88feeb0a6e2782b58f5f2d6e550f249a93f7510a`. The JSON ledger hash matches the retained ledger (`a36adadc72254e9439cb4bfb6fe05435c57350adbfb5362108de526be28c74df`). Report manifest status remains unverified because no evaluation bound host conditions; CSV contains only its header. The report was rendered again passively after the metered report activity settled so its JSON records the final budget and completed activity.

**Disposition: retain fixed Sol High.** This is a stopped, incomplete comparison. No adaptive policy qualifies or activates. A later attempt needs a new explicit execution request, a maintained release containing the fixture-export repair, fresh frozen conditions, and a new budget and policy identity. Issue #26 remains open while its comparison gates are unmet.

Published copies preserve the frozen bytes: [manifest](issue-26-v2-report/manifest.json), [calibration result](issue-26-v2-report/calibration-result.json), [HTML report](issue-26-v2-report/report.html), [JSON evidence](issue-26-v2-report/report.json), [CSV rows](issue-26-v2-report/evaluations.csv), and [findings](issue-26-v2-report/findings.md).
