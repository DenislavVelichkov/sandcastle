# Issue #31 v2 partial study

The owner-authorized replacement froze the five requested arms and 40-slot order. Calibration passed, including a verified stop and resume. The first historical preflight failed while `/tmp` had 96 free inodes; its incomplete fixture directory was retained outside the active fixture path. The host then used `TMPDIR` on the project disk and repeated preflight under the same manifest and cumulative budget. All four frozen cases passed: each base failed its protected focus with other gates passing, and each known correction passed its focus and other gates. The grader, fixture commits, model order, and manifest bytes did not change. The temporary directory relocation was an operator environment repair and was not separately bound in the manifest.

The first development slot, `stream-log-1-0` with Luna Max, was the only attempted evaluation. During its first implementation call, the same account's reported weekly window changed from 52% used to a new window at 0%, with a different reset timestamp. The controller stopped at that guard, and the slot has a verified source and session checkpoint. No reset continuation was supplied, consistent with the ticket's no-reset rule. The slot is **incomplete**; its tokens and workflow cost are unknown, and 39 slots remain unrun. There is no model ranking or candidate selection. Sol High remains the current policy.

The shared budget recorded 1,137,282 active milliseconds, including calibration, both preflight attempts, and the interrupted slot. The account readings cross different windows and cannot support a percentage-point change or cost-per-hour comparison. The report was generated passively after the stopped slot and could not be charged as a completed activity while recovery ownership remained. Its manifest status is `matched`; its one row records the incomplete slot. The HTML, JSON, CSV, manifest, ledger, budget, calibration, fixture, and slot state files here are byte-for-byte copies of the protected v2 host files. Preserve their bytes because receipt hashes bind the evidence.
