# Issue 22: native isolation and recovery, September 25, 2026

**Result: the host-managed P1/P2/P3 diagnostic passed through a durable workflow checkpoint, worker removal, restoration and a new current native check.** This is evidence for Renovio's signed-out Landing fixture. Product acceptance remained false throughout.

## Exact installation and environment

| Item              | Observed identity                                                                                                                                                                                                                                                                        |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Installed package | `@ai-hero/sandcastle@0.12.0-dv8.21.0`, installed from Renovio's pinned `package.json` and `pnpm-lock.yaml` release URL with `pnpm install --frozen-lockfile`                                                                                                                             |
| Immutable release | [`v0.12.0-dv8.21.0`](https://github.com/DenislavVelichkov/sandcastle/releases/tag/v0.12.0-dv8.21.0), source `46aae5d7278be702f395c3f2c0f2631dfda19c13`, archive SHA-256 `f3e3801822a171a15ebed9be4b972cfa2c3545cbe87bd12261ad613b711f8cb2`; GitHub release and asset verification passed |
| Renovio candidate | `27e2c757224718d9d9d3dc295a0c47fb3451be90`; clean worktree at capture and validation                                                                                                                                                                                                     |
| Android build     | `io.renovio.mobile` version `1` / `1.0`, APK SHA-256 `477bee04ca63f6fa7cc67ea432f6656eb27d1a5b5de4887d3894ee0ffb8dd642`, signing certificate SHA-256 `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c` on all devices                                                   |
| Metro             | Renovio `frontend/mobile`, `http://127.0.0.1:18081`, running and bound to the same configuration in the capture and applicability receipts                                                                                                                                               |
| Devices           | P1 `renovio_pilot_api36` / `emulator-5554`; P2 `renovio_pilot_api26` / `emulator-5556` with 200% font and dark mode; P3 `renovio_pilot_tablet_api36` / `emulator-5558` with Bulgarian app locale. All three used the required launcher and NVIDIA hardware GLES.                         |
| Runner            | Maestro 2.10.0, installed from the official release ZIP after SHA-256 verification. The project owner has separate ports 7201–7203 and a 20-minute operation deadline.                                                                                                                   |

The first attempt, `3a8881a7-60eb-48bc-9f65-ca55377c4702`, stopped before capture because the preinstalled Maestro 2.5.1 lacked the required concurrent runner port option. The option is hidden in Maestro 2.10.0, so Renovio commit `27e2c757` changed its readiness check to test actual option parsing. The next attempt, `af0e90b4-eeab-430d-a763-c200d1ae5f35`, reached the device runner but found existing signed-in sessions. All three devices were signed out through the app UI; no app data was cleared. Both failed attempts retained the host lock until their owner process, project lease and runner ports were checked stopped and the installed recovery API released it.

## Final native operation

The documented Renovio command ran through `scripts/sandcastle-native-proof-check.mjs`, its project owner, and the installed `runNativeProof()` export. Operation `e3f7b43c-d769-487c-a4e0-cba4e53c20e4` passed with two profiles active at most, three capture attempts, three successful new observations and no failed or avoidable replay. Behavior logs show real navigation from Landing to owner and contractor login and back, visible localized content and actions, then a signed-out relaunch on each device. The owner saved three native screenshots and behavior logs; its independent validator observed current Landing hierarchies and rechecked source, APK, Metro, device profiles and artifact hashes.

The original files remain under Renovio `tmp/pilot-dev-proof/exercises/e3f7b43c-d769-487c-a4e0-cba4e53c20e4/`. Exact copies of the [receipt](issue-22-evidence/receipt.json), [applicability record](issue-22-evidence/applicability.json), [comparison](issue-22-evidence/comparison.json) and each profile's screenshot and behavior log are retained in [issue-22-evidence](issue-22-evidence/). The receipt has SHA-256 `21db2e72f770b8d5bedfce687a67156937e28000da71e755f6cd8addad2b82ec`. The independent applicability record has SHA-256 `ded7a54ef841be2b11ad8bba0243da11c8612f227bf4cf85dc7c3f1946eaf95e` and observed the three current profiles at `2026-09-25T19:55:13.177Z`. The receipt binds each capture and behavior log by SHA-256. Its P1 fixture control matched, the deliberately wrong digest mismatched, and both control and receipt say `productAcceptance: false`. This comparison does not select a production reference or promote a baseline.

An earlier successful operation, `07f99c18-49c7-4386-b4a3-8f4282221a3d`, was independently revalidated from saved captures with a new applicability receipt before the recovery exercise. This proves current-state reuse without recapture. The previously published P1/P2 execution remains only a two-profile result; this run is the first recorded full P1/P2/P3 matrix through the installed host binding.

While Renovio held the host lock, a separate Sandcastle project request queued for 3,011 ms, then its own finite signal ended with `AbortError`. It did not run a project callback or change Renovio's lock owner. A separate binding operation, `49d893fb-17b0-45da-934e-b7cefe1b3abc`, was explicitly canceled after acquiring the host lock. Its process exited, the project proof lease and runner ports were absent, and `recoverNativeProofReservation()` removed that retained lock after inspection. The final operation then reacquired the host and passed. The final host lock was released normally. Focused Renovio tests (54) and its full proof suite (339) passed, including controlled mismatch and preflight checks.

After exporting the evidence, the proof's Metro server was stopped. A new applicability attempt against the saved final receipt failed with `fetch failed`, as expected without Metro; it produced no passed record. The host lock, project lease and ports 7201–7203/18081 were then absent. The passing applicability record above describes the environment while Metro was running; a future reuse must restore Metro and pass a new check.

## Durable checkpoint, removal and restoration

A new clean Renovio branch worktree started from `27e2c757`. Its exact installed dependency was prepared with `pnpm install --frozen-lockfile`, the same APK bytes were copied in, and a Sandcastle-created `sandcastle:renovio-io` Docker worker made only the deterministic fixture commit `119010f2bc598a547b96c7a76c38b74fdb458f4b`. No model was called. The native check ran on the host through the installed `runDurableWorkflow()` controller's `project.check`, Renovio's `checkNativeLanding()` owner binding and the real P1/P2/P3 devices. Operation `cf3b9356-e3a1-4e08-b131-d6ccfe922552` passed. The project acceptance callback deliberately returned `blocked`, and the controller retained the project reservation with `productAcceptance: false`.

The controller stopped with checkpoint `e3977b45-5596-40f7-aebd-9062aff5b04f`, manifest SHA-256 `f8c0b67ff36b53f3bbf93381e405ba9950163dda7cc0cee7cca915285e6f00f7`. Its [state](issue-22-evidence/durable/state.json) records `lifecycle: stopped`, the passed project check, a retained reservation, and verified source restoration. The [checkpoint manifest](issue-22-evidence/durable/checkpoint-manifest.json) binds the candidate branch and commit, 2,573 source entries and the owner's receipt and applicability artifacts. `checkpointStopWorkflow()` verified the stopped checkpoint. The Docker worker was then closed and absent from `docker ps`; the native operation's output directory was removed. `recoverDurableWorkflow()` restored the source and ignored output, verified the reservation, and returned `lifecycle: stopped` with `sourceRestoration: verified`. The restored [receipt](issue-22-evidence/durable/pre-recovery/receipt.json) retained SHA-256 `afeabc0a82b68b53e5ad2461468c1798dc4f95289404036e5fdc24a40ff2094c`; the restored [applicability record](issue-22-evidence/durable/pre-recovery/applicability.json) retained SHA-256 `e89499d15e304321fcaf65f274610c9f4df25caacc2de5532fbd523f4ee6998d`. Both match the checkpoint manifest. The original screenshots, behavior logs and comparison are exported beside them.

Recovery rewrote tracked source files after Metro had started, so the owner's timestamp check rejected that Metro process. Restarting Metro made the old receipt's exact process binding stale. The owner correctly rejected reuse; no passing applicability record was written for the stale capture. A new operation, `395d8d10-3bd5-4bf0-b286-7a8df874d068`, then ran against the restored clean commit and new Metro process. It passed fresh P1/P2/P3 capture, diagnostic comparison and current-state validation through the same installed host binding. Its [receipt](issue-22-evidence/durable/post-recovery/receipt.json) has SHA-256 `a2c2f4e42e8afef757f0f493582b5e0e2b18e2885897db008af51977529b11f2`; its [applicability record](issue-22-evidence/durable/post-recovery/applicability.json) has SHA-256 `0bda6a9c636f483a156ede05971bf73d0e0b56777586d6ea2afc78fe9ef21ed9`. The three new screenshots, behavior logs and comparison are exported beside them. The separate earlier operation demonstrates saved-capture reuse only while its exact Metro process remained live. After exporting, Metro was stopped; the native host lock, project proof lease, runner ports 7201–7203, Metro port 18081 and Sandcastle Docker worker were all absent. The candidate worktree remained clean for retained checkpoint inspection.

## Limits

The diagnostic validates native element visibility and a fresh hierarchy for each profile. It does not exercise TalkBack focus/order, approve P2 large-text layout, or make a product visual claim. The deterministic fixture exercised checkpoint and native owner recovery, not model-session recovery; `sessionRestoration: verified` means this invocation had no required agent session. The checkpoint's full blobs and retained project reservation remain in host state at `~/.local/state/sandcastle/issue-22-durable-9d10153b-f2e4-49c6-b5fb-a15de1205c9c/`. Future reuse requires a fresh owner check of current source, APK, Metro, fixture and devices; a restarted Metro requires a new capture under the current exact-process contract.
