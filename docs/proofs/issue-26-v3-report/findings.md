# Issue #26: third bounded pilot

The owner-authorized third attempt froze manifest SHA-256 `69da9bf54e347d83a5c4bf050cbd90462710fd93f6b6254ff5bdb22a8a502ab0` and policy `issue26-seven-arm-v3`. It installed the sealed `v0.12.0-dv8.23.0` release (source `c784396c038ed9a41f4634df4e926e21e71ac5da`, archive SHA-256 `ed63d1161f232321df28b804e4d0174633ce34b35f0bbe9cf73cb2b5974960ae`). The host entry was committed as `afce6d2` before freezing. The actual worker catalog included all seven frozen settings.

The four-hour host clock began before calibration preparation. Calibration passed in four model calls and 298,289 ms of controller active time, below its six-call and 15-minute limits. A live checkpoint restored both source and session; implementation and both required reviews completed, and project acceptance passed. The interrupted first implementation call has unknown token cost. The other three calls have complete counters, so no complete calibration token total is claimed. The account's seven-day window showed 40% used before and after; that coarse equality does not prove zero cost.

Historical preflight stopped on the first `stream-log` base before any comparison evaluation. The installed exporter recreated the exact historical Git tree, but the protected grader's install command could not start because the host lacked the executable it expected. The grader returned an environment failure with empty command output, and the exporter rejected the base preflight. A separate, disposable diagnostic confirmed this missing executable. With the historical tool available only in that diagnostic, the base failed its focused test and passed ordinary gates, while the known correction passed both. Those offline diagnostic results do **not** replace the frozen attempt's failed preflight.

The shared budget records 298,741 ms of active work, four measurement calls, zero evaluations and no unfinished invocation. All 64 comparison slots remain unrun: 28 fixed development, 28 fixed held-out and eight adaptive. No pair was frozen and there is no ranking or savings estimate. **Disposition: retain fixed Sol High; no policy activation.** This is a stopped, incomplete comparison.

The installed renderer produced the [HTML report](report.html), [JSON evidence](report.json) and [CSV rows](evaluations.csv). Its ledger hash is `22f7b91ebdc56f2e6ee829f670b594d07fdf5631407e718a0af04a74d1b74270`. The manifest status is unverified because no scored evaluation bound the host conditions. CSV contains only its header. The retained [manifest](manifest.json), [calibration result](calibration-result.json), [budget](budget.json) and [ledger](benchmark.json) keep the frozen state and unknown usage.

After this stop, the host grader and a distinct, unexecuted fourth pilot entry were changed to use pnpm only. The pnpm path imports each historical package lock, installs its resolved dependencies with hoisting, removes generated metadata, and runs checks with pnpm. An offline disposable base and known-correction check both passed their expected protected grading outcomes. The worker image was prepared with pnpm 11.19.0 from [its Dockerfile](../../scripts/issue-26-worker.Dockerfile). These later changes do not alter or validate the stopped third pilot. A fresh execution requires a separate explicit request, new frozen conditions, budget and policy identity.
