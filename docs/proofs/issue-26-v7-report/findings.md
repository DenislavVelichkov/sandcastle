# Issue #26: seventh bounded pilot

**Disposition: retain fixed Sol High; no policy activation.** This was a stopped bounded operation and an incomplete comparison. One development slot ran, no held-out or adaptive slot ran, and no model ranking or subscription-savings estimate can be supported.

The owner-authorized entry froze policy `issue26-seven-arm-v7` in the [manifest](manifest.json), SHA-256 `5851f4657c6a6964fed68afa23b9718303ddf384a0e6c60a7496b881fe927b43`. It bound the sealed `v0.12.0-dv8.23.0` release from source `c784396c038ed9a41f4634df4e926e21e71ac5da`, worker image `sha256:fd16475df746edadc2ed06e5ae06b338f7169fb9be79f3ce0b9defcc91b93c4e`, private worker configuration, seven worker-available settings, four historical commit pairs, protected grader, role policy, 64 counterbalanced slots and account. The [host clock](accounting.json) started at 2026-09-26 12:12:16 UTC before [calibration preparation](calibration-preparation.json). The report verifies the manifest as **matched** for the recorded evaluation.

[Calibration](calibration-result.json) passed with four model calls and 298,944 ms of controller active time, within its six-call and 15-minute caps. It verified a live checkpoint, source and session restoration, resumed Sol High settings, accepted implementation, both required reviews and cancellation accounting. The interrupted first implementation call has unknown attributable tokens, so a complete calibration token total is unavailable.

The protected [historical preflight](fixtures.json) passed for all four cases: `stream-log`, `output-retry`, `merge-to-head` and `sandbox-handle`. Each base failed its focused check while ordinary gates passed; each known correction passed both focused and ordinary checks. No case was replaced or silently substituted.

The first frozen development slot, `stream-log-1-0` (Luna Max), used a fresh worktree and session. Its implementation committed a candidate. Standards review passed; specification review rejected it because a raw chunk ending in `\r` could leave the following structured log entry on the same LF-delimited line. The controller recorded the slot as **incomplete** with reason `specification-review did not pass`, did not accept the candidate or run protected grading on it, and retained `stream-log-1-0` as recovery owner. No further slot started. The slot recorded three model calls and 644,262 ms of task active time, with complete token-counter coverage. This is a reviewed candidate failure, not evidence that the historical task is unsolvable by Luna Max.

The [budget](budget.json) records 1,245,951 ms of cumulative controller active work, four calibration calls, one attempted evaluation, and an unfinished pilot invocation. The [ledger](benchmark.json) hash is `2dcf2d9f0f7da91e42d3a2437f13c324b6a70a55b04454b5d0a1eeecb7b211a4`. The primary account window read 42% used at baseline and at the last recorded reading. That coarse unchanged reading does not establish zero cost. The host cannot exclude unrelated shared-account activity, so per-evaluation subscription cost is incomparable. The 20% conservative savings gate, selected development pair, held-out rule and repetition-direction check are all untested. The other 63 slots remain unrun: 27 fixed development, 28 fixed held-out and eight adaptive.

The installed renderer produced the [HTML report](report.html), [JSON evidence](report.json) and [CSV rows](evaluations.csv) passively because the unfinished invocation prevented a report reservation. Their SHA-256 values are `4b0e5c0f5f3b91fcbe41898c1df01000bbe2206f6d2a7851a7dd555faae8595e`, `90c18e1acb368998419d29bbd31e64d599bab87ae64ab365a6be278056f475e4` and `b93f893d3a100139e017af69ac473d384818bd46439423425f08ee7ec11667de`. The report records exactly one incomplete row. Passive rendering is not counted as controller active time. Its manifest status is matched, while its comparison remains incomplete.

The full comparison and held-out promotion gates remain unmet. A later run would require a separately frozen entry and a cost-comparable account window; this stopped run cannot be extended by changing its prompts, graders or outcome. Fixed Sol High remains the definitive policy disposition for the next task.
