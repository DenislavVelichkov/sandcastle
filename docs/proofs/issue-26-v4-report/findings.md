# Issue #26: fourth bounded pilot

The owner-authorized fourth attempt froze policy `issue26-seven-arm-v4` in the [manifest](manifest.json), SHA-256 `6407207922bae2602d83afa29585dd19eba9529d896a58fd2089a9a56c133f84`. It used the sealed `v0.12.0-dv8.23.0` release, source `c784396c038ed9a41f4634df4e926e21e71ac5da`, the pnpm-only host grader and the worker image `sha256:fd16475df746edadc2ed06e5ae06b338f7169fb9be79f3ce0b9defcc91b93c4e`. The actual worker catalog contained all seven frozen settings. The four-hour [host clock](accounting.json) began at 2026-09-26 11:13:10 UTC, before [2,899 ms of calibration preparation](calibration-preparation.json).

[Calibration](calibration-result.json) passed in four model calls and 309,753 ms of controller active time, below its six-call and 15-minute caps. The live checkpoint restored source and session state; implementation and both required reviews completed, and the task was accepted. The interrupted first implementation call has unknown attributable token cost. Later calls have complete counters, but a complete calibration token total cannot be claimed. The account's coarse seven-day window showed 41% used before and after; this does not establish zero consumption.

Historical preflight stopped on the `output-retry` known correction. The `stream-log` case had completed, and the `output-retry` base had reached its expected focused failure, but the correction did not pass the frozen protected grader. A disposable reproduction found the cause: the grader copied the candidate into an archive-based shadow without `.git`, while the focused `src/run.test.ts` tests exercise Git worktrees. Its ordinary checks passed, but three focused tests failed with `fatal: not a git repository`. The subsequent grader repair initializes and commits the isolated shadow before focused tests. Offline checks with that repair found the expected focused failure and ordinary success for all four historical bases, and focus plus ordinary success for all four known corrections. The [grader smoke check](../../scripts/issue-26-grader-smoke.mjs) reproduces the corrected `output-retry` result. These later checks do **not** alter the frozen fourth attempt or its failed preflight.

The [budget](budget.json) records 469,664 ms of controller active work: 309,753 ms calibration, 159,878 ms failed preflight and 33 ms metered report generation. It records four measurement calls, zero evaluations and no unfinished invocation. The [ledger](benchmark.json) hash is `3b49ebfeee1da4bde5180a83e2d64d01619fcaf245e8952cfcdf6e55f9924f1e`. All 64 comparison slots remain unrun: 28 fixed development, 28 fixed held-out and eight adaptive. No pair was frozen; no ranking or savings estimate exists.

The installed renderer produced the [HTML report](report.html), [JSON evidence](report.json) and [CSV rows](evaluations.csv). Their SHA-256 values are `65417e524fdb566b6b209f5cf9d356489ed9c431511ddfa2471b6058c722f832`, `71287e035cd2ea469eef8d614af6cef31ec5f54761e065c6e4388d788c366400` and `9c781f65b6368d0b85ab078e88feeb0a6e2782b58f5f2d6e550f249a93f7510a`. The report was passively rerendered after the metered activity settled, so its JSON includes the final budget and ledger. Its manifest status is **unverified** because no scored evaluation bound host conditions. Its CSV contains only the header.

**Disposition: retain fixed Sol High; no policy activation.** This is a stopped, incomplete comparison. A fifth entry is prepared with a distinct policy, owner directory and budget identity, but has no frozen manifest or model calls. Running it requires a separate explicit request. Issue #26 remains open until historical preflight, the bounded comparison and held-out assessment pass under a newly frozen attempt.
