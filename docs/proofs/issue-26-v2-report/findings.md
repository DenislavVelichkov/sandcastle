# Sandcastle issue #26: second bounded pilot

**Outcome: stopped at historical preflight. Keep fixed Sol High.** The seven-configuration comparison did not start. No adaptive policy was qualified or activated.

The owner-authorized follow-up used a fresh policy (`issue26-seven-arm-v2`) and frozen manifest (`12ddbc78355ac5fe556ab4ec65453162c9119d02b730bbe47cb5301ae302d5c3`). It ran against the published `@ai-hero/sandcastle` `0.12.0-dv8.22.0` artifact, source commit `b56bd6c26039e99946f0bafaaee1b334b4c11f5d`, the frozen worker image, Codex CLI, account, seven settings, historical commits, prompts, roles, grader bytes, and slot order. The complete manifest is retained beside this report at `../manifest.json`.

The host clock began at 2026-09-26 10:06:42 UTC, before 2,253 ms of preparation. Calibration used four calls and 286,563 ms of active time. It captured a verified live checkpoint, resumed the same implementation session, completed both reviews, and accepted the calibration task. The canceled first call has unknown token coverage; three later calls have complete counters. Total calibration tokens remain unattributable because of that canceled call. The seven-day account window read 39% before and after calibration at one-percentage-point resolution; that does not establish zero cost.

Preflight failed before the first evaluation. The historical `stream-log` base includes tracked `.sandcastle/.env.example`, but its ignore rule `.env.*` hid the file when the installed exporter rebuilt a clean Git tree. The exporter correctly rejected the differing tree. The ledger records 148 ms for this failed activity. No case was replaced and no model call followed the preflight failure. The repository source now fixes the exporter, but the frozen installed release used here does not contain that fix.

All 64 slots are unrun: 28 fixed development, 28 fixed held-out, and 8 adaptive. No base-failure/known-correction preflight pair, model ranking, savings figure, adaptive pair, or held-out promotion decision is supported. Controller active time after the metered report is 286,743 ms; preparation is recorded separately. The companion `report.html`, `report.json`, and `evaluations.csv` are native renderer outputs from the retained ledger. The JSON contains the final settled budget and failed preflight activity; the CSV has only a header. The manifest is marked unverified in the renderer because no evaluation froze host conditions. A passive final render after metered reporting settled is not included in controller active time.

This report is an incomplete-operation record. A future comparison requires a new explicit execution request and a maintained release with the exporter fix, then fresh frozen conditions and a distinct budget/policy identity.
